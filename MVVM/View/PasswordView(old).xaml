 ﻿<UserControl x:Class="PassManaAlpha.MVVM.View.PasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PassManaAlpha.MVVM.View"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#943566">

    <UserControl.Resources>
        <local:BoolToEyeConverter x:Key="BoolToEyeConverter"/>

        <Style x:Key="VaultTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1E1B2E"/>
            <Setter Property="Foreground" Value="#E8D5E8"/>
            <Setter Property="CaretBrush" Value="#c084fc"/>
            <Setter Property="BorderBrush" Value="#4a3460"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#c084fc"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#a38ca3"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="2,0,0,4"/>
        </Style>

        <Style x:Key="EyeBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2a1f3a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#FF403850">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="14,14,8,14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#2D1F3D" CornerRadius="10" Padding="16">
                <StackPanel>
                    <TextBlock Text="Add Entry" Foreground="White" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,14" ToolTip="STOP!"/>
                    <TextBlock Text="NAME" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource VaultTextBox}" Text="{Binding InputTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                    <TextBlock Text="USERNAME" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource VaultTextBox}" Text="{Binding InputUsername, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                    <TextBlock Text="PASSWORD" Style="{StaticResource FieldLabel}"/>
                    <TextBox Style="{StaticResource VaultTextBox}" Text="{Binding InputPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Style="{StaticResource LocalTypical}" Command="{Binding SaveCommand}" Height="32" Margin="0,0,4,0">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="ContentSave" Width="13" Height="13" Foreground="White" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Save" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Grid.Column="1" Style="{StaticResource LocalTypical}" Command="{Binding LoadCommand}" IsEnabled="{Binding IsLoadEnabled}" Height="32" Margin="4,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="LockOpen" Width="13" Height="13" Foreground="White" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="Load" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Background="#1E1B2E" CornerRadius="10" Margin="0,10,0,0" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="12,10,8,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="console" Foreground="#4a3460" FontFamily="Consolas" FontSize="11" VerticalAlignment="Center" ToolTip="Shows I/O"/>
                        <Button Grid.Column="1" Style="{StaticResource LocalTypical}" ToolTip="Clear console" Command="{Binding ClearConsoleCommand}" FontFamily="Consolas" FontSize="10" Height="20" Padding="6,0" Background="Transparent" Foreground="#4a3460">
                            <iconPacks:PackIconMaterial Kind="TrashCanOutline" Width="11" Height="11" Foreground="#4a3460"/>
                        </Button>
                    </Grid>

                    <TextBox Grid.Row="1" Text="{Binding ConsoleLog}" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" FontFamily="Consolas" FontSize="11" Background="Transparent" Foreground="#FFA195AE" BorderThickness="0" AcceptsReturn="True" Padding="12,8"/>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Column="1" Margin="8,14,14,14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Vault" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Command="{Binding ReloadCommand}" FontSize="12" Background="Transparent" Foreground="#FF2D1F3D" Opacity="0.5" Margin="0,0,8,0" ToolTip="Offload current loaded vault">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Refresh" Width="13" Height="13" Foreground="#FF2D1F3D" VerticalAlignment="Center" Margin="0,0,0,0"/>
                    </StackPanel>
                </Button>
                <TextBlock Grid.Column="2" Text="Right-click an entry for options" FontSize="11" VerticalAlignment="Center" Foreground="#FF8261A3"/>
            </Grid>

            <Border Grid.Row="1" CornerRadius="10" ClipToBounds="True">
                <ListView x:Name="VaultListView" ItemsSource="{Binding Entries}" Background="#2D1F3D" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="EntryCard" Background="#1E1B2E" CornerRadius="8" Padding="14,10" Margin="8,4" MouseEnter="EntryBorder_MouseActivity" MouseMove="EntryBorder_MouseActivity" MouseLeave="EntryBorder_MouseLeave" MouseLeftButtonDown="EntryBorder_MouseLeftButtonDown" Tag="{Binding}">
                                <Border.ContextMenu>
                                    <ContextMenu Tag="{Binding}" Opened="ContextMenu_Opened">
                                        <MenuItem Header="Copy name"     Tag="title"    Click="ContextMenu_Click"/>
                                        <MenuItem Header="Copy username" Tag="username" Click="ContextMenu_Click"/>
                                        <MenuItem Header="Copy password" Tag="password" Click="ContextMenu_Click"/>
                                        <Separator/>
                                        <MenuItem Header="Copy all"      Tag="all"      Click="ContextMenu_Click"/>
                                        <Separator/>
                                        <MenuItem Header="Delete"        Tag="delete"   Click="ContextMenu_Click" Foreground="#E05555"/>
                                    </ContextMenu>
                                </Border.ContextMenu>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="13" Foreground="White"/>
                                        <TextBlock Text="{Binding Username}" FontSize="11" Foreground="#7a6a8a" Margin="0,2,0,0"/>
                                        <TextBlock Text="{Binding Password}" FontSize="11" Foreground="#5a4a6a" Margin="0,1,0,0">
                                            <TextBlock.Effect>
                                                <BlurEffect Radius="{Binding PasswordBlur}"/>
                                            </TextBlock.Effect>
                                        </TextBlock>
                                    </StackPanel>
                                    <Button Grid.Column="1" Style="{StaticResource EyeBtn}" Tag="{Binding}" Click="EyeButton_Click" ToolTip="Toggle password visibility" Margin="8,0,0,0" Foreground="#FF403850">
                                        <iconPacks:PackIconMaterial Kind="{Binding IsPasswordVisible, Converter={StaticResource BoolToEyeConverter}}" Width="14"  Height="14" />
                                    </Button>
                                </Grid>

                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#252235"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </Grid>
    </Grid>
</UserControl>