<UserControl x:Class="PassManaAlpha.MVVM.View.AboutView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800" 
             Background="Transparent" 
             Loaded="AboutView_Loaded">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="260"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="{Binding AppName}" FontSize="32" FontWeight="Bold" Foreground="White" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Version}" FontSize="13" Foreground="#a38ca3" Margin="0,0,0,20"/>
                <TextBlock Text="{Binding Description}" FontSize="14" Foreground="#CCCCCC" TextWrapping="Wrap" MaxWidth="340" Margin="0,0,0,24"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="Author  " Foreground="#a38ca3" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Author}" Foreground="White" FontSize="13"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="Stack   " Foreground="#a38ca3" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text="WPF · .NET 8 · AES-256 · Argon2id" Foreground="White" FontSize="13"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                    <TextBlock Text="Storage " Foreground="#a38ca3" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text="Local encrypted flat-file vault" Foreground="White" FontSize="13"/>
                </StackPanel>

                <Border BorderBrush="#40B49BD2" BorderThickness="0,1,0,0" Height="1" Width="340" HorizontalAlignment="Left" Margin="0,0,0,16"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="System  " Foreground="#a38ca3" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding WaifuName}" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="Source  " Foreground="#a38ca3" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding WaifuSeries}" Foreground="White" FontSize="13"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Artwork " Foreground="#a38ca3" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock FontSize="13">
                        <Hyperlink x:Name="Link" Foreground="#c084fc" RequestNavigate="Hyperlink_RequestNavigate" NavigateUri="https://www.pixiv.net/en/users/22932449">
                            <Run Text="Pixiv Link Reference"/>
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </StackPanel>

            <Border Grid.Column="1" CornerRadius="12" Margin="10,20,20,20" BorderBrush="#55B49BD2" BorderThickness="1">
                <Border.Background>
                    <ImageBrush ImageSource="{Binding WaifuImage}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                </Border.Background>

                <Grid>
                    <Border x:Name="DiagnosticTerminal" Background="#CC150E20" BorderBrush="#E59B84B6" BorderThickness="1" CornerRadius="12" Padding="12,10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" IsHitTestVisible="False">
                        <ScrollViewer x:Name="TerminalScroll" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <StackPanel x:Name="TerminalLogContainer" VerticalAlignment="Bottom">
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>